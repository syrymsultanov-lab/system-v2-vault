---
date: 2026-05-06
type: daily
---

# 2026-05-06 — Сессия

Шаг 1 (WF7) закрыт удалением. Шаг 2 (Notify-WF) дошёл до production-ready инфры (bot + group + test PASS), имплементация parked.

## Что сделано

### Шаг 1 — WF7 Lead-to-Contact Conversion: удалить
Аудит WF7 нашёл 6 багов (full_name vs name, отсутствие `source` NOT NULL, nested `{}` ломает parser, `status:'todo'` повтор ошибки WF4, отсутствие `type/source/priority` whitelist в task INSERT, не пишет events_log, нет `NN_node_naming`).

Сырым ответил, что в новой архитектуре конверсия лид→контакт **отменена**: лид навсегда остаётся лидом, contact → lead = будущая фича. Вместо фикса — **скип**:
- Локально `rm SYSTEM_V2_n8n_workflows_JSON/workflows/WF7_Lead_to_Contact_Conversion.json`
- VPS DELETE workflow `js4JvhaozlOd6QlJ` (active=False было, status 200 OK, verify GONE)
- `scripts/deploy_wf_from_json.py:105` — убран "WF7" из DEPLOY_ORDER
- `obsidian-vault/CLAUDE.md` строки 42, 89 — убраны упоминания WF7
- `obsidian-vault/projects/SYSTEM V2.md` — `updated: 2026-05-06`, добавлен decision-чекбокс + future feature (contact→lead path)
- Memory `project_lead_contact_directionality.md` сохранён (lead↔contact directionality + WF7 deletion context)

### Шаг 2 — Notify-WF design + bot setup
Раскопали схему `partner_settings` (6 notify-флагов, `notification_channel` CHECK email|tg|both, `quiet_from/to`) + `partner_integrations` (provider whitelist whatsapp|telegram|...). WF4 = hook-point для синхронного notify.

Решения с Сырымом (см. ниже).

Setup инфраструктуры:
- @incruises_ai_bot создан через @BotFather
- **Bot token засветился в чате** → revoke через @BotFather → `/token` → новый токен в `.env` локально
- TG-группа создана: `AI&Incruises`, chat_id `-5110729354`
- Бот добавлен в группу через ссылку `t.me/incruises_ai_bot` → профиль → "Add to Group" (стандартный поиск member не нашёл свежесозданного бота)
- Privacy mode ON у бота — `getUpdates` пустой пока member не написал `/start@incruises_ai_bot`
- После команды — chat_id вытащен через `getUpdates` API
- Test sendMessage успешен (`message_id=26`)
- `.env` локально дополнен: `TG_BOT_TOKEN`, `TG_GROUP_CHAT_ID`. Ведущий пробел в `TG_BOT_TOKEN` строке исправлен.

Имплементация parked, snapshot в `obsidian-vault/projects/Notify Workflows.md`. Создана новая memory `feedback_secrets_via_env_only.md` (после второй засветки кред в чате — Hostinger 2026-05-02, TG bot 2026-05-06).

## Решения

### Lead↔Contact directionality (final)
- **Лид навсегда остаётся лидом.** Конверсия lead → contact отменена. WF7 удалён.
- **Contact → lead = будущая фича** (когда партнёр запускает презентацию для контакта из телефонной книги, нужен workflow для создания связанного лида). Не сейчас.

### Notify-WF MVP scope
- **Каналы:** TG group `AI&Incruises` (вся команда) + Email weekly digest на `saira.sultanova@gmail.com` (Phase 2)
- **Trigger pattern:** WF4 hook (синхронный fan-out после `30_Insert_Task`), не cron-poll, не Postgres NOTIFY, не Supabase webhook (Free Plan)
- **Quiet hours игнорируем** в MVP (group chat / email — таргет сам мьютит на устройстве)
- **Events MVP:** только `notify_task`. `notify_new_lead`/`notify_reply` — потом
- **Storage:** `partner_integrations` schema готова, **но не используется в MVP** (один общий group chat, chat_id в `.env`). Per-partner личные TG → Phase 2+ (нужен onboarding flow с pairing code).
- **Email backend** — НЕ выбран (Hostinger SMTP / Gmail / Resend = решать при старте Phase 2)

### Безопасность
- **Секреты только через `.env`, никогда в чат.** Memory `feedback_secrets_via_env_only.md`. Засветки 2026-05-02 (Hostinger) и 2026-05-06 (TG bot) — паттерн.

## На следующую сессию

### Notify-WF Phase 1 — имплементация
1. **VPS env** — добавить `TG_BOT_TOKEN` + `TG_GROUP_CHAT_ID` в `/docker/n8n/.env` через Hostinger Browser Terminal, `docker compose restart n8n`
2. **WF4 patch** — нода `40_Notify_TG_Group` после `30_Insert_Task`: POST `https://api.telegram.org/bot{{$env.TG_BOT_TOKEN}}/sendMessage` с текстом задачи (если падает — task всё равно создан, не блокирующая)
3. **Redeploy** через `python scripts/upsert_wf.py WF4`
4. **E2E smoke** — тест-лид → AI квалификация → проверить TG group получил уведомление
5. **Cleanup** test data

### Notify-WF Phase 2
6. Выбрать email backend (Hostinger SMTP / Gmail app password / Resend)
7. WF15 (новый) — cron weekly, fetch tasks за неделю, render email, send Сайре
8. Schedule: день недели + время = решить

### Хвост (с прошлой сессии, всё ещё pending)
9. **`git push origin main`** — несколько локальных коммитов впереди (+ новые из этой сессии: 2 новых memory file, обновлённые CLAUDE.md/SYSTEM V2.md/Notify Workflows.md, удалённый WF7 JSON, edited deploy_wf_from_json.py)
10. **Hostinger токен ротация** — засветился 2026-05-02
11. **Phase C decisions** — 6 вопросов из `AI Agent.md` (канал MVP, KB-источник, Identity, Voice platform, бюджет, consent). Блокеры C1.

### Опциональное
12. `upsert_wf.py` auto-assign UUID — защита от webhookId quirk
13. Pre-deploy schema diff — поймал бы баги WF7 на этапе аудита (full_name vs name, отсутствующие NOT NULL колонки tasks, `status:'todo'`)

## Заметки

- **Противоречие в Business Rules.** `obsidian-vault/reference/Business Rules.md` правило 2 — "Ручной перенос контактов в лиды запрещён". Сырым сегодня сказал "контакт может стать лидом — это надо предусмотреть в будущем". Не обновляю молча — поднять в следующей сессии: правило 2 ослабить (разрешить **системную** конверсию contact→lead через workflow, ручной — всё ещё нет?). Также добавить правило "Lead навсегда остаётся лидом" (нет lead→contact).
- **Bot privacy mode ON** — стандарт у @BotFather. Для notify-only OK (бот шлёт, не читает). Если Phase 2 захочет inbound (лид пишет боту → reply → AI обрабатывает) — нужно отключать.
- **Поиск бота в `Add member` группы не работает** для свежесозданных ботов. Решение — через профиль бота "Add to Group" или прямая ссылка `t.me/incruises_ai_bot?startgroup=true`. Это foot-gun на будущее (если будем создавать ещё ботов).
- **`getUpdates` пустой пока никто не пишет команду** — privacy mode + offset state. Решение: пользователь пишет `/start@bot` или `@bot test`, потом GET с `offset=-1`.
- **WF7 был мёртвым кодом ~7 дней** (с 2026-04-29 пометки до 2026-05-06 удаления). Никакие другие WFs не звали его, в `app.js` не было ссылок. Production impact: ноль.
- **2 новых memory** в этой сессии: `project_lead_contact_directionality.md`, `feedback_secrets_via_env_only.md`. MEMORY.md index обновлён.
- **Tree:** ~12 файлов uncommitted (5 WF JSON с прошлой сессии + 1 удалённый WF7 + app.js + CLAUDE.md + SYSTEM V2.md + Notify Workflows.md + 2 daily + .env + deploy_wf_from_json.py + новые memory). `git push` действительно нужен.
