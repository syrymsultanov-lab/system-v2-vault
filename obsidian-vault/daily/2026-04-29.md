---
date: 2026-04-29
type: daily
---

# 2026-04-29 — Сессия

## Что сделано

### Зачистка БД и n8n
- **Темирбек Мурзагалиев удалён** из `leads` (был тестовый, связанных записей не было). После чистки `leads=0`
- **Дропнута таблица `lead_status_log`** — была пустая, использовалась только в документации vault, в коде нигде. Внешних FK на неё не было. Осталось **21 таблица из 22** (max 25)
- **Зачищены 34 старых workflow в n8n** через DELETE API: дубликаты WF1, все WF2-WF22 со старого Supabase, `WF_Error_Handler`, `Lead Qualification Pipeline v1`. В n8n осталось 9 WF: 7 активных боевых (WF2/3/4/5/6/9/12) + `WF1 — Lead Intake` inactive (backup) + `WF7 — Lead to Contact Conversion` inactive (ждёт решения)

### Аудит "PROMPT_FIX_LANDING_DASHBOARD.md"
Claude Chat сослался на промт из своей памяти, который **не существует** в репо. Проверил 5 пунктов оттуда — **все уже закрыты** реально:
1. 7 полей в `leads` → есть с 2026-04-19
2. `db.js` маппинг → `db.js` нет, есть `api.js`/`app.js`, E2E прогон формы пройден
3. `login.html` мёртвый Supabase URL → актуальный `njwraxmlzglmofxiwmxs.supabase.co` с publishable key
4. Dashboard грузит первого партнёра → `api.js:43` `partners?user_id=eq.${user.id}` берёт авторизованного
5. RLS на `leads`/`events_log`/`lead_status_log` → RLS включён + own-only политики, `lead_status_log` дропнут

### FTP-деплой на Hostinger (14 файлов)
До деплоя прод устарел на 8 дней (с коммита 805405d). Залил:
- `assets/js/app.js` — главное: форма теперь POST напрямую в `/rest/v1/leads`, не через n8n
- `dashboard/assets/js/api.js` — **новый файл, на проде его не было**, без него все 9 вкладок дашборда были мертвы
- `index.html`, `login.html`
- 9 файлов дашборда: `dashboard.html`, `leads.html`, `contacts.html`, `tasks.html`, `structure.html`, `history.html`, `training.html`, `templates.html`, `settings.html`
- `dashboard/assets/js/dashboard.js`, `dashboard/assets/css/dashboard.css`

Sanity-чек на проде: `app.js` содержит `fetch(...rest/v1/leads...)`, `api.js` присутствует.

### style.css: прод-версия принята как канон
- Локальный (= git) `style.css` = 882 строки. Прод = 933 строки. Полностью разные файлы
- Скачал прод-версию, скопировал в репо `assets/css/style.css` (вариант 1 из трёх)
- Прод-версия содержала **готовую инфраструктуру для фонов** (`.section-bg`, `.bg-pool/suite/restaurant/port`), которая никогда не применялась в HTML

### Фоновые картинки в 4 секции лендинга
Применил классы `section-bg bg-*` в `index.html`:
- PROBLEMS (`#about`) → `bg-port`
- FEATURES (`#features`) → `bg-suite`
- 3 STEPS → `bg-restaurant`
- COMPARISON → `bg-pool`

Тёмный градиентный оверлей встроен в `.section-bg::before`. На десктопе — parallax (`background-attachment: fixed`).

### Удалена секция IMAGE GALLERY
«Мечта ближе, чем кажется» с 4 `<img>` (pool/suite/restaurant/port) — удалена из `index.html`. Картинки теперь работают как фоны, плитка не нужна. Прод-версия `index.html` стала 29210 байт (было 31125).

## Решения

- **Темирбек — тестовый**, удалён
- **`lead_status_log` дропнута**, оставляем `lead_events` как единственный журнал событий
- **35 старых WF в n8n** → зачищены 34 (один из них уже был backup `BLf7zJhSslBPBxwN`)
- **`style.css` прод-версия → канон**: содержит более новую инфраструктуру стилей, локальная git-версия откатилась бы на месяц назад
- **Фоны картинок в 4 секциях лендинга** + удаление дублирующей галереи

## На следующую сессию

### Критический путь
1. **E2E прогон формы** на реальном `sairateam.com` (как Сайра-партнёр):
   - Форма отправляется → лид появился в `leads`
   - Триггер БД создал `ai_jobs` со статусом `queued`
   - Сайра видит лид в дашборде во вкладке Заявки
2. **Дебаг WF3** — почему ai_jobs застревали в `running` (OpenAI API key? RPC `claim_next_ai_job`?). Открыть execution в n8n UI, найти где валится
3. **Закоммитить хвост** — правки 24-29 апреля:
   - `M assets/js/app.js`
   - `M assets/css/style.css` (прод-версия)
   - `M index.html` (фоны + удалена галерея)
   - `M obsidian-vault/CLAUDE.md`
   - `M obsidian-vault/projects/SYSTEM V2.md`
   - `M SYSTEM_V2_n8n_workflows_JSON/workflows/WF1_Lead_Intake.json`
   - `?? scripts/` + новые daily

### Открытые вопросы
- **WF7** — что делает: webhook `POST /system-v2/lead-convert` → создаёт `contact` из `lead`, патчит `leads.status='converted'`, пишет `lead_events`, создаёт onboarding-task. Триггеров запуска нет, кнопки в дашборде нет. Решение: включить inactive / удалить из n8n / удалить везде. **Сырым ответ не дал**
- В прод-CSS обнаружен баг WF7: пишет `full_name` в `contacts`, а в схеме поле называется `name` — починить если решим использовать

### После запуска
- Уведомление партнёра о новом лиде (не реализовано)
- Onboarding-инструкция для Сайры
- WF8/WF10/WF11 (daily_metrics, RAG knowledge_sources/chunks) — нужны 3-4 таблицы из 4 свободных слотов

## Заметки

- **Claude Chat работает с устаревшей памятью.** Сослался на промт `PROMPT_FIX_LANDING_DASHBOARD.md` который не существует. Все 5 «проблем» из него закрыты месяц назад. Урок: не доверять Claude Chat как источнику правды о состоянии — всегда верифицировать кодом и БД
- **Hostinger хранит auto-backup**: при заливке поверх существующего файла (по крайней мере для index.html и style.css) сохраняет старую версию как `*_old`. Полезно для восстановления, но имена не датированы
- **Прод-CSS эволюционировал отдельно от git** на 51 строку. Кто-то правил `style.css` прямо на сервере (или через Antigravity?) и не возвращал в репо. **Урок: периодически забирать прод обратно**, иначе локальный коммит может стереть месяцы работы
- **Инфраструктура `.section-bg` в CSS существовала, но не применялась в HTML.** Задумка дизайна, до которой не доходили руки. Сегодня применил
- **n8n: ~35 старых WF реально были 36** (вместе с дубликатами). Все деактивированы и не выполнялись, удалить было безопасно
