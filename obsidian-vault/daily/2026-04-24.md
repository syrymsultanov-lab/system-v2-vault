---
date: 2026-04-24
type: daily
---

# 2026-04-24 — Сессия

## Что сделано

### Подключение n8n
- Добавил `N8N_API_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `OPENAI_API_KEY`, `SUPABASE_URL` в `.env`
- Подтвердили доступ к n8n API (200 OK на `GET /api/v1/workflows`)
- Обнаружил: в n8n уже было ~35 старых workflows — все inactive, все указывали на **старый Supabase проект** `huhwkryymkqeyilpvxlx` (мёртвый)

### Обнаружены готовые JSON-файлы workflows
- В `SYSTEM_V2_n8n_workflows_JSON/workflows/` нашли 12 спроектированных workflows (WF1–WF12)
- Они используют `$env.SUPABASE_URL`, `$env.SUPABASE_SERVICE_ROLE_KEY`, `$env.OPENAI_API_KEY` — правильный подход
- Решили: JSONs — эталон дизайна; БД приводим под них, не наоборот

### Миграция БД (align_schema_with_wf_json_design)
Добавлены 3 новые таблицы (теперь **20/20 — лимит достигнут**):
- `lead_events` — события по лиду (WF1, WF2 пишут сюда)
- `ai_recommendations` — рекомендации AI (WF3 пишет сюда)
- `ai_job_runs` — лог прогонов AI job (WF3 пишет сюда)

Изменения в существующих таблицах:
- `ai_jobs.job_type` constraint: `['qualify','present']` → `['qualify_lead','qualify_contact','present','follow_up','respond']`
- `ai_jobs.status` constraint: `['pending','running','done','failed']` → `['queued','running','succeeded','failed']`
- `ai_jobs` + колонка `payload jsonb`
- `leads` + колонка `score integer`
- Триггер `create_qualification_job()` обновлён: теперь пишет `job_type='qualify_lead'`, `status='queued'`
- `claim_next_ai_job()` обновлён под новые статусы

### Деплой WF3 в n8n
- Написал Python-скрипт `scripts/create_wf_qualification.py` для деплоя через n8n API
- Решил проблему резолвинга `$env.*` внутри n8n-выражений:
  - Чистые `={{$env.VAR}}` → plain string
  - Смешанные `={{$env.VAR + $json.id}}` → `={{'value' + $json.id}}` (сохраняет runtime часть)
- WF3 задеплоен и активирован (`id=0Jcp6O31CJhMdpex`), schedule = каждые 2 минуты

### E2E тест — в процессе
На момент завершения сессии тестовый лид `60294e78-452d-433e-a8f1-3caa2bb55dd3` создан, `ai_job` в статусе `queued`. Фоновый опрос запущен, результат неизвестен.

## Решения

- **JSONs в `SYSTEM_V2_n8n_workflows_JSON/` — канонический источник workflows.** Не переписывать с нуля — деплоить оттуда через Python-скрипт с инлайн-резолвингом `$env.*`
- **Нумерация нод n8n:** формат `NN_node_name` (01_, 10_, 20_...) — делать сразу при генерации
- **20 таблиц — лимит достигнут.** Новые таблицы только после удаления ненужных или апгрейда плана
- **SUPABASE_SERVICE_ROLE_KEY** (не `SERVICE_KEY`) — точное имя в `.env`
- **n8n Variables API — 403** (недоступен на текущем плане). `$env.*` читается только из process env. Обходной путь: инлайн-резолвинг при деплое через API

## На следующую сессию

1. **Проверить результат E2E теста WF3** — открыть n8n UI → WF3 → последний execution, посмотреть прошёл ли полный цикл (`succeeded` в `ai_jobs`, `score` обновился в `leads`)
2. **Если WF3 работает** → деплоить оставшиеся нужные workflows:
   - WF1 Lead Intake (webhook с лендинга)
   - WF2 Lead Dedup (каждые 5 мин)
3. **Подключить WF1 к лендингу** — решить: лендинг пишет напрямую в Supabase (текущее) или через n8n webhook (WF1). Уточнить у Сырыма
4. **Убрать старые ~35 неактивных workflows** из n8n (мусор от старого проекта)
5. **Почистить тестовые лиды** из `leads` и `ai_jobs`

## Заметки

- **`$env.*` в n8n**: Variables API — 403. Env vars нужно либо ставить на VPS через SSH, либо инлайнить при деплое. Второй вариант и используется — скрипт в `scripts/`.
- **Старые workflows в n8n (~35 штук)** — все inactive, все на мёртвом проекте. Не трогали — просто добавили новый. В следующей сессии зачистить.
- **Двойственность `lead_events` vs `lead_status_log`**: в WF-JSONs используется `lead_events` (создали). `lead_status_log` остался — дублирует функцию. Решить: оставить одну или использовать обе под разные цели.
- **n8n `fullResponse: true`** в WF-JSONs — ответы приходят в `$json.body`, а не в `$json` напрямую. Важно при написании кода в Code нодах.
- **Скрипт деплоя** `scripts/create_wf_qualification.py` — рабочий шаблон для деплоя любого WF JSON через API. Переиспользовать.

---

# 2026-04-24 — Сессия 2

## Что сделано

### Анализ очерёдности WF
- Разобрали все 12 WF: триггеры, входные/выходные таблицы, зависимости
- Выявили 5 независимых цепочек: Лиды (WF1→WF3→WF4+WF2), Сообщения (WF5→WF6→WF9), Конверсия (WF7), RAG (WF10→WF11), Аналитика (WF8+WF12)
- WF5/WF6/WF9 заблокированы: нужны `inbound_messages` и `outbound_messages`

### Апгрейд лимита таблиц (Вариант А)
- Лимит таблиц был самоустановленным (поле `max-tables` в SYSTEM V2.md), не платформенным
- Создали 2 таблицы: `inbound_messages` (9 колонок, WF5 пишет/WF6 читает) и `outbound_messages` (9 колонок, WF9 читает approved)
- Обе с RLS own-only. Лимит поднят: `20 → 25`, счётчик: `20 → 22`
- SYSTEM V2.md и CLAUDE.md обновлены

### Деплой WF через универсальный скрипт
- Написан `scripts/deploy_wf_from_json.py` — принимает список WF-префиксов, инлайнит `$env.*`, POST + activate
- Задеплоены и активированы: WF2, WF4, WF5, WF6, WF9, WF12
- WF1 и WF7 были SKIP (уже существовали в n8n, но оказались INACTIVE — скрипт не активировал)
- Итого активных: **7** (не 8 и не 9, как я ошибочно заявил)

### Исправление WF1 и переключение лендинга
- Обнаружено: существующий WF1 в n8n (путь `test-lead`) — устаревший, не из нашего JSON
- Удалён старый WF1, задеплоен из `WF1_Lead_Intake.json`
- **Критический баг в WF1 JSON**: использовал `full_name`, а таблица `leads` имеет `name` + `last_name` — несовпадение
- Исправлен `WF1_Lead_Intake.json`: Normalize Input и Insert Lead теперь принимают и пишут `name`, `last_name`, `country`, `city`, `messenger`, `messenger_handle`, `consent`, `consent_at`
- WF1 активирован (`id=BLf7zJhSslBPBxwN`), webhook: `https://n8n.sairateam.com/webhook/system-v2/lead-intake`
- `assets/js/app.js` → `submitForm`: POST теперь идёт на WF1 webhook, Supabase-заголовки убраны. `SUPABASE_URL`/`ANON_KEY` остались (нужны для валидации ref-кода)

## Решения

- **Лендинг → WF1 webhook** (не прямая запись в Supabase). Закрыто окончательно
- **Лимит таблиц Supabase** — самоустановленный, не платформенный. Поднят до 25. Supabase Free не ограничивает кол-во таблиц
- **WF8/WF10/WF11** — отложены. WF8 нужна `daily_metrics`, WF10/WF11 — `knowledge_sources`/`knowledge_chunks`. Осталось 3 слота из 25
- **WF7** — оставлен inactive. Webhook on-demand, активировать при необходимости

## На следующую сессию

1. **Тест формы локально** (`http://127.0.0.1:3000/`) — проверить CORS от n8n (`n8n.sairateam.com` → `sairateam.com`)
2. **Если CORS ок** — деплой `assets/js/app.js` на Hostinger через FTP
3. **E2E тест полного цикла**: форма → WF1 webhook → `leads` → `ai_jobs` queued → WF3 подхватывает → `qualified`
4. **Зачистить мусор в n8n**: ~35 старых inactive WF со старого проекта + 4 дубликата WF1
5. **Почистить тестовые лиды** из `leads` и `ai_jobs`
6. **Проверить E2E WF3** из прошлой сессии (тестовый лид `60294e78...` — неизвестен результат)

## Заметки

- **`deploy_wf_from_json.py`** — универсальный скрипт для деплоя из JSON. Работает. Баг: `active` — read-only при создании, убрали из payload. Баг: `→` в ошибке ломал cp1251 консоль Windows, заменили на `->`
- **WF1 был в n8n с неправильным путём** (`test-lead` вместо `system-v2/lead-intake`) — старый артефакт. Скрипт SKIPнул его по совпадению имени. Урок: SKIP без проверки пути/конфига небезопасен
- **8 активных WF** (WF1, WF2, WF3, WF4, WF5, WF6, WF9, WF12) — WF7 inactive намеренно
- **Возможный CORS**: n8n webhook на `n8n.sairateam.com`, лендинг на `sairateam.com`. Нужно проверить настройки CORS в n8n на VPS перед деплоем
- **WF1 JSON обновлён** — canonical source теперь синхронизирован с реальной схемой БД
