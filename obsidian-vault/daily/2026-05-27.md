---
date: 2026-05-27
type: daily
---

# 2026-05-27 — Сессия

## Что сделано

### 1. 🔴 Кейс «Салтанат Кенигесова» — dedup miss

Сырым: «Салтанат партнёр Сайры, но зарегистрировалась как лид через форму. Её не пустили к боту, и сам бот не вышел на связь».

**Аудит DB:**
- 2 лида с одинаковым телефоном но разными форматами:
  - `b84e8627` (12:42) phone `77080277396` (без +), handle `@Saltavip`, last_name «Керигесрва» (typo)
  - `014d3c23` (13:27) phone `+77080277396`, handle `@Saltavipstar`, last_name «Кенигесова»
- 2 контакта auto-created (trigger `create_contact_from_lead`), оба `tg_chat_id=NULL`, ai_consent=true
- 2 outbound `status='dry_run'`, `sent_at=NULL` — бот не может писать первым по handle (нужен numeric chat_id)

**Корни:**
1. WF1 `Normalize Input` jsCode: `phone.replace(/[^\d+]/g, '')` — не нормализует к E.164. Два формата → две строки → dedup промазал
2. TG Bot API физически не пишет первым по `@handle` (известное ограничение, deep-link bridge не реализован)

**Между запросами Сырым удалил lead `b84e8627` вручную из dashboard** (direct SELECT по UUID вернул []). Contact `ddb9ea1c` @Saltavip остался orphan.

### 2. 🔴 Fix WF1 — phone E.164 normalization

`scripts/patch_wf1_phone_e164_dedup.py` (idempotent) — патчит `Normalize Input` jsCode WF1:
```js
let digits = clean(body.phone).replace(/\D/g, '');
if (digits.length === 11 && digits.startsWith('8')) digits = '7' + digits.slice(1);
const phone = digits ? '+' + digits : '';
```

**Verify (node-локально):**
| Input | Output |
|---|---|
| `77080277396` | `+77080277396` |
| `+77080277396` | `+77080277396` |
| `+7 (708) 027-7396` | `+77080277396` |
| `8 708 027 7396` | `+77080277396` |
| `87080277396` | `+77080277396` |

Все 5 вариантов → одна каноническая строка. Future dedup `Find Existing Lead` поймает повтор.

### 3. Backfill leads/contacts phone

Audit: 2 лида в БД (после удаления `b84e8627`), оба уже `+77...` E.164. `leads_to_fix=0`. Contacts: 8 строк, у form-derived `phone=NULL` (trigger `create_contact_from_lead` не копирует phone в contacts). `contacts_to_fix=0`.

**Discoveries:**
- contact Батимы `c026566b` имеет `messenger_handle='+77089419616'` — phone попал в handle field (trigger bug)
- contacts schema не имеет `last_name` — только `name`

### 4. 🟢 Импорт контактов из файла — feature implementation

Discovery: `openImport()` в `dashboard/contacts.html` был stub (`showToast('отдельная итерация')`). Аналогично `openAddContact`, `editContact`.

**v1 (initial):**
- Modal с file input (`.csv,.vcf`)
- CSV parser (поддерживает `,/;/\t`, quoted fields, RU+EN headers — `Имя/Телефон/Мессенджер/...`)
- VCF parser (BEGIN:VCARD blocks, FN/N/TEL/EMAIL/X-TELEGRAM, params strip)
- E.164 normalize (same logic as WF1 fix)
- Detect messenger: `@x` → telegram, digits → whatsapp, `x@y.z` → email
- Preview table: status (✅/⚠), name, phone, handle, source
- Dedup vs existing CONTACTS in memory (phone OR handle match)
- Bulk POST `contacts` через `sb('POST', ...)`
- Source = 'csv'/'vcf', imported_from = filename

Smoke parsers (Node): CSV 4 row + VCF 3 row → все phones корректно → E.164. PASS.

Test fixtures: `.tmp/import_test/test.csv` (RU headers, KZ 8-prefix, quoted field), `.tmp/import_test/test.vcf` (3 vCards).

**v2 (после теста Сырыма):**
- Editable preview rows — каждое поле input (`name`, `phone`, `handle`), tag — dropdown
- ✕/↩ кнопка на каждой строке — пропустить/вернуть
- Inline phone normalize при редактировании
- Bulk-skip кнопки в preview: «🗑 без тел / без handle / без email / дубли»

**v3 (после уточнения Saira workflow):**
Сырым: «Сайра загружает 5700 контактов из телефона, фильтрует, просматривает всё, выбирает кого надо, потом удаляет».

- Multi-filter chip row под тегами: `🚫 без имени · без фамилии · без тел · без email · без handle · без тега` (multi-select toggle)
- «Нет фамилии» парсится из `name` (< 2 слов через split /\s+/)
- Checkbox на каждой строке (click на body — модал; click на checkbox — select)
- Bulk-bar над списком: `Показано N · Выделено M · ✓ Всё видимое · ↕ Инвертировать · ✕ Снять · 🗑 Удалить M` (последняя кнопка красная)
- Chunked POST импорт по 500/batch с progress (`⏳ 1500/5700…`)
- Chunked DELETE по 500/batch
- `loadContacts` GET с `limit=20000` (было default ~1000)

3 деплоя через `scripts/ftp_deploy_contacts_import.py` на Hostinger.

### 5. Templates/Training discovery
| Таблица | Rows |
|---|---|
| `templates` | 0 |
| `training_modules` | 0 |
| `training_lessons` | 0 |
| `training_progress` | 0 |

UI обеих вкладок = stubs (`openNewTpl`, `editTpl`, player). Контент не существовал. Ждёт диктовки от Сырыма или ingestion из vault.

## Решения

- **Phone canonical = E.164 (`+` + digits)** — для leads + contacts + outbound. Нормализуется на input (WF1, dashboard import)
- **Saira workflow для import 5700 контактов**: загрузить ВСЕ в DB → фильтры пропусков → multi-checkbox review → DELETE selected. НЕ автоматический bulk-delete по фильтру. Сайра решает каждую запись (или групповым select-all-visible)
- **«Нет фамилии» = name.split(/\s+/).length < 2** — без миграции схемы. Если позже нужен формальный `last_name` — отдельная задача
- **lead `b84e8627` удалён вручную Сырымом** — orphan contact `ddb9ea1c` + 2 dry_run outbounds остались, cleanup отложен
- **Open-access TG bridge остаётся закрытым** — Салтанат не «пустили к боту» потому что DB-only mode (закрыли 2026-05-25). Это работает as designed для security, но не решает её случай — она и должна была получить proactive greeting через WF9, который зависает без `tg_chat_id`

## На следующую сессию

### 🔴 Smoke 5700 Saira VCF
Сайра экспортирует контакты из телефона → загружает через dashboard. Verify:
- Парс VCF без OOM
- Chunked POST progress visible
- Filter chips работают на 5700 строк
- Render не лагает (если лаг — добавить virtualization)
- Multi-select + chunked DELETE OK

### 🔴 Form → TG bot deep-link bridge (вариант C)
Корневая фикс для 3 form-лидов в dry_run (Салтанат×2, Батима):
- После submit формы → redirect `t.me/incruises_ai_bot?start=<lead_uuid>`
- Бот при `/start <uuid>` → lookup lead → UPDATE contact `tg_chat_id` + `messenger_handle`
- WF9 dispatch greeting срабатывает

Без этого все form-лиды зависают как Салтанат.

### 🔴 Templates + Training seed
Ждёт от Сырыма:
- Список шаблонов (TG greeting, WA follow-up, презентация, реактивация)
- Список training modules (Onboarding 101RU, презентация, возражения, AI-bot usage)
ИЛИ pull из vault (есть `WhatsApp Outreach Template.md`, для training контента нет)

### 🔴 Pricing smoke retest (перенос с 2026-05-25)
3 Qs Сырыма с chat_id 5243912117 (PREMIUM split, 2000 RP/$2600, STARTER активация). Verify canonical `Membership Pricing.md` override держится в RAG.

### 🔴 Saira interview Block_2-8
Background task `bbo7vnowc` (статус неизвестен), Block_1 ffmpeg downsample, разбор на 5 doc-файлов.

### 🟡 Cleanup orphan Saltanat артефакты
- contact `ddb9ea1c` @Saltavip (lead удалён, контакт сирота)
- outbound `96d2fa70` dry_run @Saltavip
- outbound `ecce3f7b` dry_run @Saltavipstar (контакт ещё валиден)

### 🟡 Trigger `create_contact_from_lead` schema bugs
- Не копирует `phone` в contacts (поле существует, но триггер не пишет)
- Контакт Батимы получил `messenger_handle='+77089419616'` (phone в handle field)

### 🟡 Single edit form для контактов
`editContact()` всё ещё stub. После import + bulk-delete юзер захочет редактировать отдельные строки уже в БД.

## Заметки

- ⚠ **Schema drift**: contacts не имеет `last_name`, только `name`. UI «нет фамилии» парсится из строки. Если нужно отделить фамилию для импорта — миграция + парсер «Иван Петров» → name='Иван', last_name='Петров'
- ⚠ **TG Bot API ограничение** — бот не может писать первым по @handle/phone, только по numeric chat_id. Это physical limit Telegram, не наш bug. Решение = deep-link (push к юзеру через ссылку, юзер жмёт Start, бот ловит chat_id)
- ⚠ **5 form-лидов** теперь в системе (2 Салтанат + Батима + ?), все с dry_run. Без deep-link bridge — funnel «форма → AI» сломан для всех новых form-лидов
- ⚠ **Сырым удалил lead `b84e8627` вручную через dashboard** — это **первое подтверждение** что delete-кнопка в leads UI работает. Также значит что cascade на contacts НЕ работает (контакт остался). Если важно — добавить FK ON DELETE CASCADE или trigger cleanup
- ⚠ **Каждый второй чат «не предлагать yes/no — действовать»** срабатывает: Сырым явно сказал «делай», я строил без подтверждения. 3 итерации contacts.html (initial → editable preview → multi-select) — это нормальный flow для UI features
- ⚠ Дашборд НЕ имеет authentication для дублей session — Сырым залогинился как saira.sultanova и импорт ушёл под её partner_id (27c3a628). Это ожидаемо если он использует Сайрин аккаунт для теста, но если хотел свой — нужен logout + login снова

## Связи

- [[SYSTEM V2]] — добавлен contact import feature + WF1 dedup fix
- [[2026-05-25]] — DB-only access (контекст почему Салтанат «не пустили»)
- [[2026-05-23]] — open-access bridge (когда form-лиды Салтанат появились)
