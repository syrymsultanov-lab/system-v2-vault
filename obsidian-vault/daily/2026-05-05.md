---
date: 2026-05-05
type: daily
---

# 2026-05-05 — Сессия

Закрытие intake debt + WF1 Plan B switchover. Прогон по плану из 2026-05-04 evening: задачи 1-4.

## Что сделано

### Task 1 — WF1 `channel` bug fix
`Insert Lead` шлёт `channel:$json.channel`, в `leads` колонки `channel` нет. Убрано из jsonBody (line 126 WF1 JSON). Normalize node `channel` поле сохранено (попадает в lead_events payload). Redeploy через `upsert_wf.py WF1`. webhookId UUID `7f5e07e0-...` сохранился.

### Task 2 — E2E smoke (5 итераций, каскад багов)
Curl → webhook → проверять executions API + DB. Каждая итерация ловила следующий слой багов:

1. **52720**: `null value in column "name"`. Причина: `Insert Lead` использует `$json.name`, но после `Find Existing Lead` (HTTP node) `$json` = HTTP response, не Normalize output. Fix: ссылаться на `$('Normalize Input').item.json.X` через IIFE.
2. **52722**: Insert Lead прошла. Падает `Insert Lead Event` — "invalid syntax". Причина: nested `{...payload:{mode:...}}` в `={{ }}` ломает parser (memory rule 10). Fix: literal JSON template + `JSON.stringify`.
3. **52726**: `Queue AI Job` — "JSON parameter needs to be valid JSON". Причина та же что в 52720 — `$json.lead_id` undefined после промежуточной HTTP ноды. Fix: `$('Extract Lead Id').isExecuted ? ... : $('Use Existing Lead')` IIFE с conditional чтобы покрыть обе ветки (insert и use-existing).
4. **52730**: `Queue AI Job` — `null value in column "lead_id"`. Memory `reference_ai_jobs_schema.md` показывал что lead_id NOT NULL — проглядел при первом проходе. **Решение: удалить ноду Queue AI Job** (триггер `create_qualification_job` уже создаёт ai_jobs queued автоматом — Queue AI Job в WF1 = дубль). Это часть scope task 4 (Plan B), вынесено вперёд.
5. **52735**: SUCCESS. Webhook respond `{"ok":true}` HTTP 200. lead + lead_events(1) + events_log(1) + ai_jobs(queued, src='trigger') — цепочка работает.

**Cleanup smoke leads:** 4 partial-state лида (b, c, d, e) с каскадом (3 lead_events, 1 events_log, 4 ai_jobs, 4 ai_recs, 3 tasks). Phase B chain отработала на тесте → 3 фейк-task для Сайры. Удалено всё каскадно.

### Task 3 — webhookId sweep
**Local sweep (`SYSTEM_V2_n8n_workflows_JSON/workflows/*.json`):** 4 WFs без webhookId — WF5, WF7, WF10, WF11. WF1 OK.

**Live n8n sweep:**
- WF1: OK
- **WF5 active=True но webhookId=None → 404 на любых входящих** (production bug)
- WF7: prod webhookId=`wf7-convert` (literal string), active=False
- WF10/WF11: не задеплоены

UUID назначены локально:
- WF5: `bba9514b-ad58-49a3-a873-66fabc0b6639`
- WF7: `b7d920d7-b982-468d-9983-563edaadbac9`
- WF10: `a6d3957a-6f67-4b0c-bba3-fdbf90feae67`
- WF11: `ab0664e5-4e5a-4184-98f8-604243bd0481`

WF5 redeployed → live webhookId привязан → curl `/webhook/system-v2/inbound-message` теперь HTTP 200 (было 404). Production bug закрыт.

### Task 4 — WF1 Plan B switchover
- `assets/js/app.js`: новая константа `LEAD_INTAKE_WEBHOOK_URL`, `submitForm` POST на `https://n8n.sairateam.com/webhook/system-v2/lead-intake`. Headers: только `Content-Type`. Убраны `apikey`/`Authorization`/`Prefer`. `formData`: убран `status:'new'` (WF1 ставит сам), добавлен `channel:'web'`.
- FTP upload `app.js` на Hostinger `public_html/assets/js/app.js` (HTTP 226, 7360 bytes).
- Verify через `curl https://sairateam.com/assets/js/app.js` — содержит новую константу, нет `/rest/v1/leads`.
- E2E POST на webhook → `{ok:true}` HTTP 200, full chain ✓.
- Cleanup e2e лида (cascade delete tasks/recs/jobs/events_log/lead_events/leads). Leads count back to baseline=3.

### Vault sync
- `CLAUDE.md`: AI-pipeline диаграмма переписана (`landing form (app.js POST) → WF1 webhook /system-v2/lead-intake → Normalize → Find Existing → ... → respond {ok:true}`). TODO секция полностью обновлена — Plan B убран, добавлены остатки (WF7, Notify-WF, git push, токен ротация, Phase C decisions).
- `SYSTEM V2.md`: WF1 Plan B чекбокс отмечен done, frontmatter `updated: 2026-05-05`.
- Memory: новый `feedback_n8n_json_after_http_node.md` (паттерн `$json` после HTTP ноды), удалён `project_wf1_plan_b_pending.md` + запись из `MEMORY.md`.

## Решения

- **Удалить Queue AI Job ноду из WF1.** Триггер `create_qualification_job` уже создаёт ai_jobs queued автоматически. Дубль не нужен. Это решение перенесено из task 4 в task 2 — упростило прогон smoke (вместо очередного fix).
- **Plan B switchover делать через POST без `Prefer: return=*` headers.** Webhook возвращает `{ok:true}` через respond node, минимальный hop.
- **WF5 redeploy в одиночку.** Был активный production bug (404). Не дожидаться следующих task'ов.
- **Cleanup всех тестовых лидов в конце сессии.** Чтобы Phase B chain не оставила фейк-task'и в дашборде Сайры. Подтверждено юзером ("ok").
- **`channel:'web'` оставить в Normalize WF1**, убрать только из `Insert Lead` body. Поле уходит в `lead_events.payload`/идентификации, но не в `leads` (нет колонки).
- **WF7/10/11 webhookId защитно (UUID локально).** Не задеплоены, но при будущем deploy не споткнутся о quirk. WF7 prod `wf7-convert` будет переписан на UUID при следующем deploy — без impact, т.к. URL зависит от `path`, не от webhookId.

## На следующую сессию

### Закрытие хвоста intake
1. **WF7 Lead-to-Contact Conversion** — висит со 2026-04-29. Webhook node теперь имеет UUID, но workflow active=False. Нужно: проверить логику конверсии lead→contact, активировать, E2E.

### Phase C boundary
2. **Notify-WF design.** Сейчас Сайра не получает уведомлений когда AI создаёт task. `partner_settings.notification_channel='tg'` дефолт (но никто не читает). Дизайн: WF12 cron / WF1 hook / events_log listener?

### Чистый ремоут + cleanup
3. **`git push origin main`** — несколько коммитов локально впереди. Ждать confirm.
4. **Hostinger токен ротация** — засветлён в чате 2026-05-02. Сгенерить новый, обновить `.env`, FTP_PASS.

### Phase C decisions (блокеры C1)
5. **6 вопросов из `AI Agent.md`** — Канал MVP (TG/WA), KB-источник, Identity policy, Voice platform, Бюджет, Consent tracking. Без ответа C1 не стартует.

### Опциональные улучшения
6. **`upsert_wf.py` auto-assign UUID** — если webhook node без webhookId, генерить `uuid.uuid4()` перед PUT. Защита от регрессии. Memory note 2026-05-04 evening.
7. **Pre-deploy schema diff** — простой скрипт «WF JSON jsonBody → information_schema.columns diff» перед каждым PUT. Ловил бы `channel` bug, `status:'todo'` в WF4, и т.п.

## Заметки

- **n8n `$json` после HTTP node** — рекуррентный foot-gun. Каждая HTTP-нода в середине chain «съедает» upstream context. Memory `feedback_n8n_json_after_http_node.md` добавлен. Cherry-pick: при цепочке `Code → HTTP → ...` всегда использовать `$('Code Node').item.json.X`, не `$json.X`.
- **Каскад багов в WF1** оказался не один bug, а 5 разных: channel, $json после HTTP × 3 нод, nested `{}`, дубль ai_jobs. Каждая итерация прогон → exec error → fix. Время потрачено разумно: каждый шаг открывал новую ошибку только когда предыдущая закрыта.
- **Memory `reference_ai_jobs_schema.md` — был верен** (lead_id в списке колонок). Я проглядел `is_nullable: NO` на lead_id. Сверка с `information_schema` напрямую дала точный ответ. Урок: не оптимизировать на скорость — лезть в схему перед написанием jsonBody с FK.
- **Production bug WF5 webhookId=None active=True** жил с момента squash-коммита `efdf896` (2026-05-04). Никто не вызывал `/system-v2/inbound-message` → не видно. Sweep сегодня нашёл. Pattern: при PUT через REST API webhook node остаётся с `null webhookId` → 404. Memory `reference_n8n_webhook_id_quirk.md` упоминает.
- **HTTP 200 ≠ workflow succeeded** — повтор урока 2026-05-04. webhook responds 200 при `responseMode:'responseNode'` независимо от downstream нод (если не lastNodeExecuted в IF false). Always check executions API после curl smoke.
- **% выполнения SYSTEM v2** — Intake side ~95% (хвост = WF7, Notify-WF). Vision side ~35-40%. Phase C впереди.
- **Tree:** 8 файлов uncommitted (5 WF JSON, app.js, CLAUDE.md, SYSTEM V2.md). Локально несколько коммитов впереди origin/main.
