---
date: 2026-05-04
type: daily
session: evening
---

# 2026-05-04 (вечер) — Сессия

Продолжение утренней сессии (см. `2026-05-04.md`). Фокус: закрыть хвосты, security audit БД, vault sync, попытка live E2E.

## Что сделано

### Squash коммит `efdf896`
28 файлов, +2627/-311. Объединил весь некоммитнутый хвост:
- WF3 v3 + WF4 v2 + WF13 (Phase B chain)
- WF1/2/12 events_log fixes (`event_type → event`, `actor='system'`)
- WF1 lead INSERT расширенные поля (name, last_name, country, city, messenger, consent, status='new')
- Landing visual refresh (section-bg классы, `OWNER_REF_CODE: TEST001 → SAIRA001`, scroll fix)
- `scripts/upsert_wf.py` + 2 helper deploy скрипта
- Vault: CLAUDE.md (21→19 таблиц, Phase B диаграмма, rules 9-10), AI Agent.md (Phase C planning + MVP rollout C1-C6), 8 daily notes 2026-04-24..05-04, InCruises Knowledge Base stub, AI Agent Questionnaire
- Удалён дубль `PROMT_CLEANING_CLAUDE_WEEKLE.md` (оставлен короткий `_CLEAN_`)
- Также включён pre-fix WF1 webhookId (см. ниже) и догрузка `SYSTEM V2.md` synced

### Security audit БД + lockdown миграция
Новая миграция `20260504_lockdown_security_definer_functions`:
```sql
REVOKE EXECUTE ON FUNCTION public.claim_next_ai_job(text)      FROM anon, authenticated, public;
REVOKE EXECUTE ON FUNCTION public.reset_stuck_ai_jobs(integer) FROM anon, authenticated, public;
REVOKE EXECUTE ON FUNCTION public.create_qualification_job()   FROM anon, authenticated, public;
REVOKE EXECUTE ON FUNCTION public.handle_new_auth_user()       FROM anon, authenticated, public;
REVOKE EXECUTE ON FUNCTION public.handle_new_partner()         FROM anon, authenticated, public;
REVOKE EXECUTE ON FUNCTION public.rls_auto_enable()            FROM anon, authenticated, public;
ALTER FUNCTION public.create_qualification_job() SET search_path = public;
```

**Результат:** advisor security warns 16 → 3.

Оставшиеся 3 — intentional/нефиксимые:
- `events_log INSERT WITH CHECK (true)` для anon+auth (n8n + landing пишут события) — закроется после WF1 Plan B
- `leads INSERT WITH CHECK (true)` для anon (форма лендинга) — закроется после WF1 Plan B
- `auth_leaked_password_protection` — Pro Plan only, проект на Free → игнор

### n8n VPS sync verify
Pulled live JSON для WF1/WF2/WF12, sверены ноды `events_log`. Все три на проде имеют `event` колонку и `actor='system'` ✓. Локальный репо ≡ live n8n. WF1 расширенные поля INSERT тоже deployed.

events_log на проде: 1 row `actor='system'`, `event='wf4_task_created_from_ai'` (от WF4 smoke 2026-05-04 утро) — fix работает.

### Vault sync
`SYSTEM V2.md`:
- Frontmatter `tables: 21 → 19`, `updated: 2026-05-03 → 05-04`
- Тело: список 15/22 таблиц переписан на 19 actuals (ядро 7, партнёрские 7, сообщения 2, AI-pipeline 3)
- Build status: + Phase B done, + Security lockdown done, + WF1 Plan B pending, + Phase C planning

### WF1 webhook activation fix
Live curl на `POST /webhook/system-v2/lead-intake` → 404 «not registered», хотя `active=true`. Причина: после PUT через `upsert_wf.py` поле `webhookId` сохраняется null, n8n не регистрирует production URL.

Назначил UUID `7f5e07e0-3601-4009-b57a-96b9974ad5d4` в локальном WF1 JSON, redeployed через скрипт. Webhook теперь принимает запросы (HTTP 200).

### Live E2E smoke (FAIL — найден bag)
Curl с тест-payload Сайры (`partner_id=27c3a628-...`) → HTTP 200 от вебхука, но counts в БД unchanged. Через executions API нашёл:

```
status: error, lastNodeExecuted: Insert Lead
ERROR: Bad request - please check your parameters
```

Причина: WF1 шлёт `channel:$json.channel` в `leads`. Колонки `channel` в `leads` **нет** (sверено `information_schema.columns` — 22 колонки: id, partner_id, name, phone, email, source, status, created_at, last_name, country, city, messenger, messenger_handle, consent, consent_at, priority, notes, budget, tags, assigned_at, updated_at, score).

Поле `channel` — для `lead_messages` / `inbound_messages`. Bag из commit `60008fb` (расширение Insert Lead body) — оставлено по ошибке от старой схемы.

### Memory updates
Новые:
- `reference_security_definer_lockdown.md` — что закрыто, n8n service_role, как добавлять новые SECURITY DEFINER
- `reference_supabase_plan_free.md` — Free Plan limits, какие advisor warns игнорим
- `reference_n8n_webhook_id_quirk.md` — webhookId UUID требование перед PUT, sweep команда

Обновлены:
- `project_db_schema_status.md` — security lockdown, факт что партнёров 2 (Сырым+Сайра, не 1; list_tables.rows ≈ approximate)
- `project_wf1_plan_b_pending.md` — БЛОКЕР `channel` колонка, закрытие webhookId quirk

## Решения

- **БД по intake side — закончили.** Phase A+B production-ready. Phase C-таблицы (calls/conversations/knowledge_*) — отдельный scope, не сейчас.
- **Free Plan — игнорим `auth_leaked_password_protection`** warn. Pro-only фича, апгрейд не запланирован.
- **WF1 `channel` bug — починить ПЕРЕД Plan B switchover.** Без фикса webhook chain не работает (Insert Lead 400).
- **Squash strategy** — весь сегодняшний хвост в один коммит `efdf896`. Юзер подтвердил x2 (initial + amend для SYSTEM V2.md + amend для webhookId).
- **n8n работает через `SUPABASE_SERVICE_ROLE_KEY`** во всех 13 WF (sверено grep'ом). REVOKE EXECUTE FROM anon/authenticated не влияет.

## Найдены контрадикции / расхождения

- `mcp__supabase__list_tables.rows` показывает approximate count (`pg_class.reltuples`), updates только после ANALYZE. partners=1 в выводе при реальных 2 → false alarm про orphan `partner_settings`. Memory обновлено правилом «для точных counts — execute_sql».
- `SYSTEM V2.md` устаревший (15/22 таблиц) синхронизирован с реальностью (19). Закрыто.
- `PROMT_CLEAN_CLAUDE_WEEKLE.md` ≡ `PROMT_CLEANING_CLAUDE_WEEKLE.md` (опечатка-дубль). Удалён `CLEANING`.

## На следующую сессию

### Срочное (closeout intake debt)
1. **Починить WF1 `Insert Lead` body** — убрать `channel:$json.channel` (или ALTER leads ADD COLUMN channel text — design decision). После — redeploy `python scripts/upsert_wf.py WF1`.
2. **Re-run live E2E smoke** через webhook curl (тот же payload с partner_id=27c3a628-...). Проверить: `leads` row + `lead_events` row + `events_log` row + `ai_jobs` queued → WF3 cron подхватит.
3. **Sweep остальных WF на null webhookId**: `python -c "import json,glob; [print(f, [n['name'] for n in json.load(open(f,encoding='utf-8'))['nodes'] if n.get('type')=='n8n-nodes-base.webhook' and not n.get('webhookId')]) for f in glob.glob('SYSTEM_V2_n8n_workflows_JSON/workflows/*.json')]"`. Особенно WF5 Inbound Capture.

### После фикса intake
4. **WF1 Plan B switchover** (memory `project_wf1_plan_b_pending.md`):
   - Убрать ноду `Queue AI Job` из WF1
   - `assets/js/app.js` → POST на webhook вместо прямого Supabase
   - FTP-deploy лендинга
   - E2E через `https://sairateam.com/?ref=SAIRA001`
   - Обновить CLAUDE.md
5. **WF7 Lead-to-Contact Conversion** — висит со 2026-04-29
6. **Notify-WF design** — Сайра не получает push/email когда AI создаёт task. partner_settings.notification_channel='tg' дефолт. Phase C boundary

### Чистый ремоут
7. **`git push origin main`** — 3 локальных коммита впереди (`efdf896`, `37d230b`, `805405d`). Подтвердить confirm.

### Безопасность
8. **Hostinger токен ротация** — засветлён в чате 2026-05-02 (daily). Сгенерить новый, обновить `.env`.

### Phase C decisions (блокеры C1)
9. 6 вопросов из `AI Agent.md` — Канал MVP (TG/WA), KB-источник, Identity policy, Voice platform, Бюджет, Consent tracking. Без ответа C1 не стартует.

## Заметки

- **% выполнения SYSTEM v2** (по запросу Сырыма): Intake side ~85% (хвост = WF1 channel, WF1 Plan B, WF7, notify-WF). Vision side (intake + outbound + Q&A + voice) ~35-40% — Phase C полностью впереди (12-18 сессий).
- **Pre-deploy schema check** — стоит добавить простой скрипт «WF JSON → information_schema diff» перед каждым PUT, чтобы ловить такие bug'и как `channel`. WF4 уже один раз падал на `status:'todo'` (нет колонки). Паттерн повторяется.
- **n8n PUT quirk + webhookId** — теперь решено через ручное UUID assignment. Но рискует ломать каждый раз новый WF с webhook node. `upsert_wf.py` стоит дополнить auto-assignment (если null — `uuid.uuid4()`).
- **partial-failure pattern** в n8n остаётся: HTTP 200 от webhook ≠ workflow succeeded. Always check `executions API` после curl smoke. Сегодня был обманут coup'ом status code.
- **Free Plan limits** не блокируют Phase A+B. Для Phase C5 RAG понадобится pgvector (доступен на Free), Storage (limited). Phase C6 Voice — внешний (Vapi/Retell), Plan не критичен.
- **Live E2E smoke**: тест-лид НЕ создался (INSERT 400) — счётчики leads=3 не сдвинулись. Cleanup не нужен, БД чиста. Это позитив — bug пойман до прод-данных.
- **Tree clean, локально 3 коммита впереди origin/main.**
